# ----------------------------------------
# SPRING PROPERTIES
# ----------------------------------------

# App
spring.application.name=fraud-microservice
server.servlet.context-path=${FRAUD_SERVER_SERVLET_CONTEXT_PATH}

# Database and JPA settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.default_schema=${SPRING_FLYWAY_DEFAULT_SCHEMA}

# Datasource
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Flyway
spring.flyway.schemas=${SPRING_FLYWAY_SCHEMAS}
spring.flyway.default-schema=${SPRING_FLYWAY_DEFAULT_SCHEMA}
spring.flyway.locations=classpath:db/postgresql
spring.flyway.create-schemas=true

# Actuator / Health
management.server.port=${MANAGEMENT_SERVER_PORT}
management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

# Other app-specific
fraud.limit=${FRAUD_LIMIT}
fraud.interval=${FRAUD_INTERVAL}

# Development Logging Configuration
logging.level.root=INFO
logging.level.com.areeba.cms.cmsmircoservice=DEBUG